import { PopunderDirective } from './popunder.directive';

describe('PopunderDirective', () => {
  it('should create an instance', () => {
    const directive = new PopunderDirective();
    expect(directive).toBeTruthy();
  });
});
