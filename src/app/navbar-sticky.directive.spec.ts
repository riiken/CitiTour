import { NavbarStickyDirective } from './navbar-sticky.directive';

describe('NavbarStickyDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarStickyDirective();
    expect(directive).toBeTruthy();
  });
});
